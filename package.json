{
  "name": "media-stream",
  "version": "0.1.3",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter . --parallel run build",
    "dev": "pnpm -r --filter . --parallel run dev",
    "release": "bumpp package.json packages/mixer/package.json packages/recorder/package.json --commit --push --tag && pnpm -r publish --access public"
  },
  "author": "yzydeveloper",
  "license": "MIT",
  "devDependencies": {
    "@yzydeveloper/eslint-config": "^0.1.4",
    "bumpp": "^8.2.1",
    "eslint": "^8.22.0",
    "pnpm": "^6.34.0",
    "tsup": "^5.12.9",
    "typescript": "^4.7.4"
  }
}
